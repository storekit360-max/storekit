import React, { useEffect, useState } from 'react';
import API from '../../utils/api';
export default function POSDashboard() { const [data, setData] = useState(null); const [error, setError] = useState(''); useEffect(() => { API.get('/pos/dashboard').then(r => setData(r.data)).catch(e => setError(e.response?.data?.message || 'Could not load POS dashboard')); }, []); if (error) return <div className="p-6 text-red-600">{error}</div>; if (!data) return <div className="p-6 text-gray-500">Loading POS dashboard…</div>; const cards = [['Today revenue', `Rs. ${Number(data.revenue || 0).toLocaleString()}`], ['Transactions', data.transactions], ['Average sale', `Rs. ${Number(data.averageTransaction || 0).toFixed(2)}`], ['Held sales', data.heldSales], ['Low stock', data.lowStock]]; return <div className="admin-settings min-h-full bg-slate-50 p-4 sm:p-6"><div className="mb-5"><h1 className="text-2xl font-black">POS Dashboard</h1><p className="text-sm text-gray-500">Today’s POS activity</p></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{cards.map(([label, value]) => <div key={label} className="rounded-2xl border bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase text-gray-400">{label}</p><p className="mt-2 text-2xl font-black">{value}</p></div>)}</div><div className="mt-5 rounded-2xl border bg-white p-5"><h2 className="font-black">Payment totals</h2><div className="mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{Object.entries(data.paymentTotals || {}).map(([method, value]) => <div key={method} className="rounded-xl bg-gray-50 p-3"><p className="text-xs capitalize text-gray-500">{method.replace('_', ' ')}</p><p className="font-black">Rs. {Number(value).toLocaleString()}</p></div>)}</div></div></div>; }
