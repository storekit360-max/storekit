import React,{useEffect,useState}from'react';import API from'../utils/api';
const KEY='storekit_cookie_consent';
export function analyticsConsentAllowed(policy=window.__STOREKIT_COOKIE_POLICY__||'consent_required'){if(policy==='essential_only')return false;return localStorage.getItem(KEY)==='accepted';}
export default function CookieConsent(){const[policy,setPolicy]=useState(null);const[visible,setVisible]=useState(false);useEffect(()=>{const apply=value=>{window.__STOREKIT_COOKIE_POLICY__=value;setPolicy(value);setVisible(value==='consent_required'&&!localStorage.getItem(KEY));};API.get('/platform-settings/public').then(({data})=>apply(data?.['security.cookiePolicy']||'consent_required')).catch(()=>apply('consent_required'));},[]);function choose(value){localStorage.setItem(KEY,value);if(value!=='accepted'&&window.fbq?.queue)window.fbq.queue.length=0;setVisible(false);window.dispatchEvent(new CustomEvent('storekit:cookie-consent',{detail:{value,policy}}));}
 if(!visible||policy!=='consent_required')return null;return <aside role="dialog" aria-label="Cookie preferences" className="fixed bottom-4 left-4 right-4 z-[10000] mx-auto max-w-3xl rounded-2xl border border-slate-200 bg-white p-5 shadow-2xl"><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="flex-1"><h2 className="font-extrabold text-slate-900">Your privacy choices</h2><p className="mt-1 text-sm leading-relaxed text-slate-600">StoreKit uses essential storage for sign-in and cart operation. With your permission, this store may also load analytics and advertising tools to measure sales.</p></div><div className="flex shrink-0 gap-2"><button onClick={()=>choose('essential')} className="rounded-lg border border-slate-300 px-4 py-2 text-sm font-bold text-slate-700">Essential only</button><button onClick={()=>choose('accepted')} className="rounded-lg bg-slate-900 px-4 py-2 text-sm font-bold text-white">Allow analytics</button></div></div></aside>;
}
